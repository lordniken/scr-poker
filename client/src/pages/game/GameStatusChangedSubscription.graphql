subscription GameStatusChangedSubscription($gameId: String!){
  gameStatusChanged(gameId: $gameId){
    isVotingStarted
    votingStorieId
    votedUsers{
      userId
      value
    }
  }
}