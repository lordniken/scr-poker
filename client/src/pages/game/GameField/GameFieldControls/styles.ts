import { makeStyles } from '@material-ui/core';

export const gameFieldControlsStyles = makeStyles((theme) => ({
  container: {
    padding: theme.spacing(1),
    justifyContent: 'center',
  },
}));