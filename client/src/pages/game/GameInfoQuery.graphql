query GameInfoQuery($gameId: String!){
  gameInfo(gameId: $gameId){
    gameName
    votingSystem
    cards
    allowSpectators
    isGameOwner
    votedScore
    onlineList
    status{
      isVotingStarted
      votingStorieId
    }
  }
}